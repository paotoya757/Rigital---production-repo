define(['component/_unidadDeRedMasterComponent'],function(_UnidadDeRedMasterComponent) {
    App.Component.UnidadDeRedMasterComponent = _UnidadDeRedMasterComponent.extend({
		postInit: function(){
			//Escribir en este servicio las instrucciones que desea ejecutar al inicializar el componente
		}
    });

    return App.Component.UnidadDeRedMasterComponent;
});